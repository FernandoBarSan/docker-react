sudo: required
language: generic
services: 
    - docker

before_install:
    - docker build -t fernandobarsan/docker-react -f Dockerfile.dev .

script: 
    - docker run -e CI=true fernandobarsan/docker-react npm run test